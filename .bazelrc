common --experimental_isolated_extension_usages
common --@aspect_rules_ts//ts:default_to_tsc_transpiler

# Per-contributor overrides (gitignored). See .bazelrc.local.example.
try-import %workspace%/.bazelrc.local
